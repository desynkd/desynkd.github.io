---
title: "Building a fully local RAG API"
description: "Notes on building a retrieval-augmented-generation API that never leaves the box — no cloud calls, no API keys, no data leaving your machine."
date: 2026-06-02
tags: ["rag", "llm", "python", "self-hosted"]
---

import Figure from '@/components/Figure.astro';
import placeholder1 from '@/assets/blog/placeholder-1.png';
import placeholder2 from '@/assets/blog/placeholder-2.png';
import placeholder3 from '@/assets/blog/placeholder-3.png';

Placeholder post body — proves the two-column article layout with three inline figures and a code block. Replace with the real writeup.

## Why local

Placeholder paragraph on the motivation for keeping retrieval and generation entirely on-device, without shipping user data to a third-party API.

<Figure src={placeholder1} alt="Placeholder diagram of the retrieval pipeline" caption="Request flow through the retrieval layer" />

Placeholder paragraph continuing the thought — this is the text that should sit next to the figure above once the real content lands.

## Architecture

Placeholder paragraph describing the API layer, the embedding store, and how the local model is invoked.

```python
def retrieve(query: str, k: int = 5) -> list[str]:
    embedding = embed(query)
    return vector_store.search(embedding, top_k=k)
```

<Figure src={placeholder2} alt="Placeholder diagram of the embedding store" caption="Embedding store layout" />

Placeholder paragraph on indexing strategy and chunking decisions.

## Serving

Placeholder paragraph on how the API is served — framework, concurrency model, and how responses are streamed back to the client.

<Figure src={placeholder3} alt="Placeholder diagram of the serving layer" caption="Serving layer, request to response" />

## What I'd change

Placeholder — honest notes on what didn't work and what the next iteration looks like.
